services:
  - type: web
    name: indstockscreener-api
    runtime: python
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: indstockscreener-db
          property: connectionString
      - key: CORS_ORIGINS
        sync: false   # set manually in Render dashboard after Vercel deploy
      - key: DISABLE_SELF_REFRESH
        value: "1"    # ingestion runs in GitHub Actions; Render must not self-refresh

databases:
  - name: indstockscreener-db
    databaseName: indstockscreener
    plan: free
